# Security incident containment: 16 September 2026

This commit removes the identified hidden editor Node launcher and font-disguised JavaScript payload. Automatic tasks are disabled and real environment-file exclusions restored.

Existing clones, historical commits, tags, releases and cached distributions are NOT cleared. Do not run older affected checkouts or assume pulling this commit repairs an affected machine. Preserve evidence, isolate potentially affected environments and assess persistence. Rotate accessible credentials from a known-clean device and invalidate relevant sessions.

This is targeted source containment, not proof of endpoint safety or absence of data theft. Workflow review, credential rotation, endpoint recovery and published distribution clearance remain separate required steps. Keep builds and releases paused pending review. No history rewrite or force-push is performed.
